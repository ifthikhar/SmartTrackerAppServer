import { NotetextfilterPipe } from './notetextfilter.pipe';

describe('NotetextfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NotetextfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
